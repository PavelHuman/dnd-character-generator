class Dwarf {

  constructor(
    abilityScoreIncrease, age, alignment, size, speed, languages, subraces
  ) {
    this.abilityScoreIncrease = abilityScoreIncrease,
      this.age = age,
      this.alignment = alignment,
      this.size = size,
      this.speed = speed,
      this.languages = languages,
      this.subraces = subraces
  }
}
